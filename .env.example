NEXT_PUBLIC_OPEN_TO_OPPORTUNITIES=true

# Optional — only needed for local `next dev` against real Netlify Blobs.
# On Netlify production these are injected automatically (no setup).
# NETLIFY_SITE_ID=
# NETLIFY_AUTH_TOKEN=
